import { convertSitemapToDomainModels } from './convert-sitemap-to-domain-models';

describe('convertSitemapToDomainModels', () => {
  it('should work', () => {
    expect(convertSitemapToDomainModels()).toEqual(
      'convert-sitemap-to-domain-models'
    );
  });
});
